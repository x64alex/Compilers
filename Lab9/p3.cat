main() {
	int[] a;
    int length, int index;
    length = 10;
    index = 0;

    int sum;
    sum = 0;

    while(index<length){
        sum = sum + a[index];

        index = index + 1;
    };

    print(sum);
}